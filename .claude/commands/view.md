---
description: "View details of a specific task"
argument-hint: "<task ID>"
---

Call the `get_task` tool with the provided task ID. Display the task details in a readable format showing: ID, title, status, priority, description, tags, blocked_by (with task titles if possible), retries/max_retries, result (if completed), error (if failed), and timestamps.
